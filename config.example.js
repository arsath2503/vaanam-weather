// Copy this file to config.js and paste your own key for LOCAL testing only.
// config.js is git-ignored — it will never be committed.
// For the live GitHub Pages site, the real key is injected automatically
// by the GitHub Actions workflow using a repository secret. See README.md.

window.OPENWEATHER_API_KEY = "PASTE_YOUR_KEY_HERE";
